#pragma once

#define PROJECT_NAME         "${TARGET_NAME}"
#define PROJECT_VERSION      "${${TARGET_NAME}_VERSION}"
#define PROJECT_DESCRIPTION  "${${TARGET_NAME}_DESCRIPTION}"
#define PROJECT_HOMEPAGE_URL "${${TARGET_NAME}_HOMEPAGE_URL}"
#define PROJECT_VERSION_MAJOR ${${TARGET_NAME}_VERSION_MAJOR}
#define PROJECT_VERSION_MINOR ${${TARGET_NAME}_VERSION_MINOR}
#define PROJECT_VERSION_PATCH ${${TARGET_NAME}_VERSION_PATCH}
#define PROJECT_VERSION_TWEAK ${${TARGET_NAME}_VERSION_TWEAK}
